LOADINGSCREEN_CONFIG = {
    mode = "single", -- "single" or "slideshow"

    title = "CHANGE ME",
    subtitle = "Welcome to the server!",

    displayMs = 6000,          -- slide duration (ms)
    transitionMs = 1200,       -- fade duration (ms)

    -- Music will be loaded from assets/*.mp3
    -- Add your music files to the assets folder
    musicVolume = 0.08,        -- default volume (0.0 to 1.0)
}
